<Window x:Class="Palisades.View.CreateCalendarPalisadeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Palisades.View"
        mc:Ignorable="d"
        Title="Create Calendar Palisade" MinHeight="520" Height="600" MinWidth="480" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Title" Grid.Row="0" Margin="0,0,0,4"/>
        <TextBox x:Name="PalisadeTitleTextBox" Grid.Row="1" Margin="0,0,0,10" Text="{Binding PalisadeTitle, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="CalDAV Base URL" Grid.Row="2" Margin="0,0,0,4"/>
        <TextBox x:Name="CalDAVUrlTextBox" Grid.Row="3" Margin="0,0,0,6" Text="{Binding CalDAVUrl, UpdateSourceTrigger=PropertyChanged}" ToolTip="e.g. https://mail.example.com/dav/user@domain/"/>
        <TextBlock Text="Username" Grid.Row="4" Margin="0,0,0,4"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="5" Margin="0,0,0,6" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="Password" Grid.Row="6" Margin="0,0,0,4"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="7" Margin="0,0,0,8" PasswordChanged="PasswordBox_PasswordChanged"/>
        <Button x:Name="LoadCalendarsButton" Grid.Row="8" Content="Load calendars" Margin="0,0,0,8" Click="LoadCalendarsButton_Click" HorizontalAlignment="Left"/>
        <TextBlock Text="Calendars to show" Grid.Row="9" Margin="0,0,0,4"/>
        <ListBox x:Name="CalendarsListBox" Grid.Row="10" Margin="0,0,0,8" SelectionMode="Multiple" DisplayMemberPath="DisplayName" SelectionChanged="CalendarsListBox_SelectionChanged"/>
        <StackPanel Grid.Row="11" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button x:Name="CreateButton" Content="Create" Width="80" IsDefault="True" Click="CreateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
