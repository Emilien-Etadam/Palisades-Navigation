<Window x:Class="Palisades.View.EditPalisade"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModel="clr-namespace:Palisades.ViewModel"
        Title="Edit Palisade"
        Width="400"
        SizeToContent="Height"
        TextElement.Foreground="{DynamicResource PalisadeTextBrush}"
        TextElement.FontWeight="Regular"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource PalisadeWindowBrush}"
        ResizeMode="NoResize">
    <Window.DataContext>
        <viewModel:PalisadeViewModel />
    </Window.DataContext>
    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="TextBox">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="Margin" Value="0,5" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ColumnDefinitions="Auto,10,*" RowDefinitions="Auto,*">
        <Label Grid.Row="0" FontSize="24">Name</Label>
        <TextBox FontSize="24" Grid.Row="0" Grid.Column="2" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

    </Grid>
</Window>
