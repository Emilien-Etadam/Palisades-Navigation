<Window x:Class="Palisades.View.TaskPalisadeSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Palisades.View"
        mc:Ignorable="d"
        Title="Task Palisade Settings" Height="350" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Sync Interval -->
        <TextBlock Text="Sync Interval (minutes)" Grid.Row="0" Margin="0,0,0,5"/>
        <Slider x:Name="SyncIntervalSlider" Grid.Row="1" Minimum="1" Maximum="60" 
                Value="5" TickFrequency="5" TickPlacement="BottomRight" 
                AutoToolTipPlacement="BottomRight" IsSnapToTickEnabled="True"/>

        <!-- Enable Logging -->
        <CheckBox x:Name="EnableLoggingCheckBox" Grid.Row="2" Margin="0,15,0,10"
                 Content="Enable detailed logging"/>

        <!-- Show Completed Tasks -->
        <CheckBox x:Name="ShowCompletedCheckBox" Grid.Row="3" Margin="0,0,0,10"
                 Content="Show completed tasks"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button Content="Save" Width="80" IsDefault="True" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>