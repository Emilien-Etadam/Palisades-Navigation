<Window x:Class="Palisades.View.EditFolderPortal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Palisades.View"
        xmlns:viewModel="clr-namespace:Palisades.ViewModel"
        xmlns:converters="clr-namespace:Palisades.Converters"
        mc:Ignorable="d"
        Title="Edit Folder Portal"
        Width="400"
        SizeToContent="Height"
        TextElement.Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
        TextElement.FontWeight="Regular"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
        ResizeMode="NoResize">
    <Window.DataContext>
        <viewModel:FolderPortalViewModel />
    </Window.DataContext>
    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="TextBox">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="Margin" Value="0,5,0,5" />
            </Style>
            <converters:SolidBrushToColor x:Key="brushConverter" />
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" FontSize="24">Name</Label>
        <TextBox FontSize="24" Grid.Column="2" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

        <Label Grid.Row="2" FontSize="24">Root Path</Label>
        <TextBox FontSize="14" Grid.Row="2" Grid.Column="2" Text="{Binding RootPath, Mode=OneWay}" IsReadOnly="True" HorizontalAlignment="Stretch" VerticalAlignment="Center" />

        <Label Grid.Row="4" FontSize="24">Header Color</Label>
        <local:ColorPickerButton Grid.Row="4" Grid.Column="2" SelectedColor="{Binding HeaderColor, Mode=TwoWay}" HorizontalAlignment="Center" />

        <Label Grid.Row="6" FontSize="24">Body Color</Label>
        <local:ColorPickerButton Grid.Row="6" Grid.Column="2" SelectedColor="{Binding BodyColor, Mode=TwoWay}" HorizontalAlignment="Center" />

        <Label Grid.Row="8" FontSize="24">Title Color</Label>
        <local:ColorPickerButton Grid.Row="8" Grid.Column="2" SelectedColor="{Binding TitleColor, Converter={StaticResource brushConverter}, Mode=TwoWay}" HorizontalAlignment="Center" />

        <Label Grid.Row="10" FontSize="24">Labels Color</Label>
        <local:ColorPickerButton Grid.Row="10" Grid.Column="2" SelectedColor="{Binding LabelsColor, Converter={StaticResource brushConverter}, Mode=TwoWay}" HorizontalAlignment="Center" />

    </Grid>
</Window>
