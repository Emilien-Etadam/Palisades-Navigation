<Window x:Class="Palisades.View.EditFolderPortal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModel="clr-namespace:Palisades.ViewModel"
        Title="Edit Folder Portal"
        Width="400"
        SizeToContent="Height"
        TextElement.Foreground="{DynamicResource PalisadeTextBrush}"
        TextElement.FontWeight="Regular"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource PalisadeWindowBrush}"
        ResizeMode="NoResize">
    <Window.DataContext>
        <viewModel:FolderPortalViewModel />
    </Window.DataContext>
    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="TextBox">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="Margin" Value="0,5" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ColumnDefinitions="Auto,10,*" RowDefinitions="Auto,10,Auto,*">
        <Label Grid.Row="0" FontSize="24">Name</Label>
        <TextBox FontSize="24" Grid.Column="2" Grid.Row="0" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

        <Label Grid.Row="2" FontSize="24">Root Path</Label>
        <TextBox FontSize="14" Grid.Row="2" Grid.Column="2" Text="{Binding RootPath, Mode=OneWay}" IsReadOnly="True" HorizontalAlignment="Stretch" VerticalAlignment="Center" />

    </Grid>
</Window>
