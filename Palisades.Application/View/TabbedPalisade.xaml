<Window x:Class="Palisades.View.TabbedPalisade"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Palisades.View"
        xmlns:viewModel="clr-namespace:Palisades.ViewModel"
        xmlns:helpers="clr-namespace:Palisades.Helpers"
        xmlns:converters="clr-namespace:Palisades.Converters"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
        helpers:WindowBlur.IsEnabled="True"
        helpers:WindowSinker.AlwaysOnBottom="True"
        ShowInTaskbar="False"
        mc:Ignorable="d"
        Title="Palisades"
        Width="{Binding Width, Mode=OneWay}"
        Height="{Binding Height, Mode=OneWay}"
        Left="{Binding X, Mode=OneWay}"
        Top="{Binding Y, Mode=OneWay}"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="Transparent"
        UseLayoutRounding="True">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" CornerRadius="20" GlassFrameThickness="0" NonClientFrameEdges="None" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <converters:PathToImageConverter x:Key="PathToImage" />
        <converters:MultiValueEqualityConverter x:Key="MultiValueEqualityConverter" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <helpers:BindingProxy x:Key="proxy" Data="{Binding}" />

        <!-- Style Flat : barre d'onglets uniforme -->
        <Style x:Key="FlatTabStyle" TargetType="TabControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <DockPanel>
                            <Border DockPanel.Dock="Top" Background="#E0202020" Padding="0,4,0,0">
                                <ItemsPresenter />
                            </Border>
                            <ContentPresenter Content="{TemplateBinding SelectedContent}" ContentTemplate="{TemplateBinding SelectedContentTemplate}"
                                              ContentTemplateSelector="{TemplateBinding SelectedContentTemplateSelector}" Margin="0" />
                        </DockPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FlatTabItemStyle" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Background="Transparent" Padding="12,6" Margin="2,0,0,0" Cursor="Hand">
                            <ContentPresenter ContentSource="Header" />
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#30FFFFFF" />
                                        </Trigger>
                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TabItem}}" Value="True">
                                            <Setter Property="Background" Value="#40FFFFFF" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#35FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style Rounded : onglets avec coins arrondis -->
        <Style x:Key="RoundedTabStyle" TargetType="TabControl" BasedOn="{StaticResource FlatTabStyle}" />
        <Style x:Key="RoundedTabItemStyle" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Background="#25FFFFFF" CornerRadius="6,6,0,0" Padding="12,6" Margin="2,0,0,0" Cursor="Hand">
                            <ContentPresenter ContentSource="Header" />
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#30FFFFFF" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#50FFFFFF" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Contenu Standard (PalisadeViewModel) -->
        <DataTemplate DataType="{x:Type viewModel:PalisadeViewModel}">
            <Grid AllowDrop="True">
                <Grid.Resources>
                    <helpers:BindingProxy x:Key="tabProxy" Data="{Binding}" />
                </Grid.Resources>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Drop">
                        <i:InvokeCommandAction Command="{Binding DropShortcut}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <Rectangle>
                    <Rectangle.Fill>
                        <SolidColorBrush Color="{Binding BodyColor}" />
                    </Rectangle.Fill>
                </Rectangle>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Shortcuts}" Margin="20,20,20,20" dd:DragDrop.IsDragSource="True" dd:DragDrop.IsDropTarget="True">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ContentControl>
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="MouseDoubleClick">
                                            <i:LaunchUriOrFileAction Path="{Binding UriOrFileAction}" />
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="MouseDown">
                                            <i:InvokeCommandAction Command="{Binding Path=Data.ClickShortcut, Source={StaticResource tabProxy}}" CommandParameter="{Binding}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                    <Border Margin="0,0,0,10" CornerRadius="4" Width="100">
                                        <Border.Style>
                                            <Style>
                                                <Style.Triggers>
                                                    <Trigger Property="Border.IsMouseOver" Value="True">
                                                        <Setter Property="Border.Background" Value="#20FFFFFF" />
                                                    </Trigger>
                                                    <DataTrigger Value="True">
                                                        <DataTrigger.Binding>
                                                            <MultiBinding Converter="{StaticResource MultiValueEqualityConverter}">
                                                                <Binding Mode="OneWay" />
                                                                <Binding Path="Data.SelectedShortcut" Source="{StaticResource tabProxy}" Mode="OneWay" />
                                                            </MultiBinding>
                                                        </DataTrigger.Binding>
                                                        <Setter Property="Border.Background" Value="#20FFFFFF" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" Cursor="Hand">
                                            <Image Source="{Binding IconPath, Converter={StaticResource PathToImage}}" Width="48" Height="48" Stretch="UniformToFill" Margin="5,5,5,5" />
                                            <TextBlock TextWrapping="NoWrap" TextAlignment="Center" TextTrimming="CharacterEllipsis" Text="{Binding Name}"
                                                       Foreground="{Binding DataContext.LabelsColor, RelativeSource={RelativeSource AncestorType=TabItem}}"
                                                       HorizontalAlignment="Center" Margin="5,0,5,5" />
                                        </StackPanel>
                                    </Border>
                                </ContentControl>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </DataTemplate>

        <!-- Contenu FolderPortal (FolderPortalViewModel) -->
        <DataTemplate DataType="{x:Type viewModel:FolderPortalViewModel}">
            <Grid>
                <Rectangle>
                    <Rectangle.Fill>
                        <SolidColorBrush Color="{Binding BodyColor}" />
                    </Rectangle.Fill>
                </Rectangle>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#FFFF6666" Margin="15,10" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="10">
                    <ItemsControl ItemsSource="{Binding Items}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,10" CornerRadius="4" Width="100" Cursor="Hand">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="MouseDoubleClick">
                                            <i:InvokeCommandAction Command="{Binding DataContext.NavigateIntoFolderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                    <StackPanel>
                                        <Image Source="{Binding IconPath, Converter={StaticResource PathToImage}}" Width="48" Height="48" Stretch="UniformToFill" Margin="5" />
                                        <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" Foreground="{Binding DataContext.LabelsColor, RelativeSource={RelativeSource AncestorType=TabItem}}" Margin="5,0,5,5" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </DataTemplate>

        <!-- Contenu TaskPalisade (TaskPalisadeViewModel) -->
        <DataTemplate DataType="{x:Type viewModel:TaskPalisadeViewModel}">
            <Grid>
                <Grid.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.MergedDictionaries>
                            <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
                            <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
                        </ResourceDictionary.MergedDictionaries>
                    </ResourceDictionary>
                </Grid.Resources>
                <Rectangle>
                    <Rectangle.Fill>
                        <SolidColorBrush Color="{Binding BodyColor}" />
                    </Rectangle.Fill>
                </Rectangle>
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" Margin="0,0,0,10"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Tasks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="Gray" BorderThickness="0,0,0,1" Padding="5">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox IsChecked="{Binding Completed, Mode=TwoWay}" VerticalAlignment="Center" Grid.Column="0"
                                                      Command="{Binding DataContext.ToggleTaskCompletedCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"/>
                                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                                <TextBlock Text="{Binding Title}" FontWeight="Bold" Foreground="{Binding DataContext.LabelsColor, RelativeSource={RelativeSource AncestorType=ItemsControl}}"/>
                                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="{Binding DataContext.LabelsColor, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                           Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}"/>
                                                <TextBlock Text="{Binding DueDate, StringFormat='Due: {0:d}'}" Foreground="{Binding DataContext.LabelsColor, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                           Visibility="{Binding DueDate, Converter={StaticResource StringToVisibilityConverter}}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                                <Button Content="📝" ToolTip="Edit" Command="{Binding DataContext.SaveTaskCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"
                                                        Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Width="24" Height="24" Margin="0,0,5,0"/>
                                                <Button Content="🗑" ToolTip="Delete" Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"
                                                        Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Width="24" Height="24"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <Button Grid.Row="2" Content="➕ Add Task" Command="{Binding AddTaskCommand}" HorizontalAlignment="Right" Margin="0,10,0,0"
                            Style="{StaticResource MaterialDesignFloatingActionButton}" Width="48" Height="48"/>
                </Grid>
            </Grid>
        </DataTemplate>

        <!-- Contenu Calendar (placeholder dans onglet) -->
        <DataTemplate DataType="{x:Type viewModel:CalendarPalisadeViewModel}">
            <Grid>
                <Rectangle>
                    <Rectangle.Fill>
                        <SolidColorBrush Color="{Binding BodyColor}" />
                    </Rectangle.Fill>
                </Rectangle>
                <TextBlock Text="Calendar" Foreground="{Binding LabelsColor}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" />
            </Grid>
        </DataTemplate>

        <!-- Contenu Mail (placeholder dans onglet) -->
        <DataTemplate DataType="{x:Type viewModel:MailPalisadeViewModel}">
            <Grid>
                <Rectangle>
                    <Rectangle.Fill>
                        <SolidColorBrush Color="{Binding BodyColor}" />
                    </Rectangle.Fill>
                </Rectangle>
                <TextBlock Text="Mail" Foreground="{Binding LabelsColor}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" />
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <Border x:Name="LayoutRoot" CornerRadius="20">
        <DockPanel>
            <Grid DockPanel.Dock="Top" x:Name="Header" MouseLeftButtonDown="Header_MouseLeftButtonDown" IsHitTestVisible="True" Background="#00000000">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                </Grid.ColumnDefinitions>
                <Rectangle Grid.ColumnSpan="3">
                    <Rectangle.Fill>
                        <SolidColorBrush Color="{Binding SelectedItem.HeaderColor, ElementName=TabControl}" />
                    </Rectangle.Fill>
                </Rectangle>
                <Label Grid.Column="1" Content="{Binding SelectedItem.Name, ElementName=TabControl}" Margin="5" FontSize="28" Foreground="{Binding SelectedItem.TitleColor, ElementName=TabControl}" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
            <TabControl x:Name="TabControl" ItemsSource="{Binding Members}" Style="{StaticResource FlatTabStyle}"
                        ItemContainerStyle="{StaticResource FlatTabItemStyle}"
                        SelectedIndex="0">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" Foreground="{Binding TitleColor}" />
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <ContentControl Content="{Binding}" />
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
        </DockPanel>
    </Border>
</Window>
