<Window x:Class="Palisades.View.FolderPortal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:helpers="clr-namespace:Palisades.Helpers"
        xmlns:viewModel="clr-namespace:Palisades.ViewModel"
        xmlns:converters="clr-namespace:Palisades.Converters"
        xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
        helpers:WindowBackdrop.IsEnabled="True"
        helpers:WindowSinker.AlwaysOnBottom="True"
        ShowInTaskbar="False"
        Title="FolderPortal"
        Width="{Binding Width, Mode=TwoWay}"
        Height="{Binding Height, Mode=TwoWay}"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="Transparent"
        Left="{Binding FenceX, Mode=TwoWay}"
        Top="{Binding FenceY, Mode=TwoWay}"
        UseLayoutRounding="True"
        >
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" CornerRadius="0" GlassFrameThickness="0" NonClientFrameEdges="None" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>
    <Window.DataContext>
        <viewModel:FolderPortalViewModel />
    </Window.DataContext>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/View/Styles/PalisadeStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:PathToImageConverter x:Key="PathToImage" />
            <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
            <helpers:BindingProxy x:Key="proxy" Data="{Binding}" />
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
        </ResourceDictionary>
    </Window.Resources>
    <Border x:Name="LayoutRoot" CornerRadius="0" ClipToBounds="True">
        <DockPanel>
            <Grid DockPanel.Dock="Top" x:Name="Header" MouseLeftButtonDown="Header_MouseLeftButtonDown" IsHitTestVisible="True" Background="{DynamicResource PalisadeWindowBrush}" Tag="{Binding DataContext, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}" RowDefinitions="Auto,Auto">
                <Grid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Edit" Command="{Binding Path=Data.EditFolderPortalCommand, Source={StaticResource proxy}}" CommandParameter="{Binding}" />
                        <MenuItem Header="Refresh" Command="{Binding Path=Data.RefreshCommand, Source={StaticResource proxy}}" />
                        <MenuItem Header="Open in Explorer" Command="{Binding Path=Data.OpenInExplorerCommand, Source={StaticResource proxy}}" />
                        <Separator />
                        <MenuItem Header="Delete" Command="{Binding Path=Data.DeletePalisadeCommand, Source={StaticResource proxy}}" CommandParameter="{Binding Identifier}" />
                        <Separator />
                        <MenuItem Header="New fence" Command="{Binding Path=Data.NewPalisadeCommand, Source={StaticResource proxy}}" />
                        <MenuItem Header="New Folder Portal" Command="{Binding Path=Data.NewFolderPortalCommand, Source={StaticResource proxy}}" />
                        <MenuItem Header="New Task Palisade" Command="{Binding Path=Data.NewTaskPalisadeCommand, Source={StaticResource proxy}}" />
                        <MenuItem Header="New Calendar Palisade" Command="{Binding Path=Data.NewCalendarPalisadeCommand, Source={StaticResource proxy}}" />
                        <MenuItem Header="New Mail Palisade" Command="{Binding Path=Data.NewMailPalisadeCommand, Source={StaticResource proxy}}" />
                        <Separator />
                        <MenuItem Header="Manage Zimbra Accounts" Command="{Binding Path=Data.ManageZimbraAccountsCommand, Source={StaticResource proxy}}" />
                        <Separator />
                        <MenuItem Header="Layouts" SubmenuOpened="LayoutsSubmenu_SubmenuOpened">
                            <MenuItem Header="Save current layout..." Command="{Binding Path=Data.SaveSnapshotCommand, Source={StaticResource proxy}}" />
                            <Separator />
                            <MenuItem ItemsSource="{Binding Path=Data.RecentSnapshots, Source={StaticResource proxy}}">
                                <MenuItem.ItemContainerStyle>
                                    <Style TargetType="MenuItem">
                                        <Setter Property="Header" Value="{Binding Name}" />
                                        <Setter Property="Command" Value="{Binding Path=Data.RestoreSnapshotCommand, Source={StaticResource proxy}}" />
                                        <Setter Property="CommandParameter" Value="{Binding Id}" />
                                    </Style>
                                </MenuItem.ItemContainerStyle>
                            </MenuItem>
                            <Separator />
                            <MenuItem Header="Manage layouts..." Command="{Binding Path=Data.ManageSnapshotsCommand, Source={StaticResource proxy}}" />
                        </MenuItem>
                        <Separator />
                        <MenuItem Header="About" Command="{Binding Path=Data.OpenAboutCommand, Source={StaticResource proxy}}" />
                    </ContextMenu>
                </Grid.ContextMenu>
                <Grid Grid.Row="0" Height="32" Background="{DynamicResource PalisadeWindowBrush}" ColumnDefinitions="Auto,*,Auto">
                    <Button Grid.Column="0"
                            Content="&#x2190;"
                            Command="{Binding NavigateBackCommand}"
                            ToolTip="Navigate back"
                            Style="{StaticResource PalisadeNavButton}"
                            Visibility="{Binding CanNavigateBack, Converter={StaticResource BoolToVis}}"/>
                    <TextBlock Grid.Column="1" Text="{Binding Name, Mode=OneWay}" Margin="5,0" Style="{StaticResource PalisadeHeaderText}" HorizontalAlignment="Center"/>

                    <!-- Home button -->
                    <Button Grid.Column="2"
                            Content="&#x2302;"
                            Command="{Binding NavigateToRootCommand}"
                            ToolTip="Go to root folder"
                            Style="{StaticResource PalisadeNavButton}"
                            Visibility="{Binding CanNavigateBack, Converter={StaticResource BoolToVis}}"/>
                </Grid>
                <Grid Grid.Row="1" Background="{DynamicResource PalisadeWindowBrush}">
                    <TextBlock Text="{Binding Breadcrumb, Mode=OneWay}"
                               Foreground="{DynamicResource PalisadeTextBrush}"
                               FontSize="12"
                               Margin="15,2,15,4"
                               TextTrimming="CharacterEllipsis"
                               ToolTip="{Binding Breadcrumb, Mode=OneWay}"
                               VerticalAlignment="Center" />
                </Grid>
            </Grid>
            <Border CornerRadius="0" ClipToBounds="True" Background="{DynamicResource PalisadeControlBrush}">
                <Grid x:Name="content">
                    <Grid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="New folder" Command="{Binding CreateNewFolderCommand}"/>
                            <MenuItem Header="New text file" Command="{Binding CreateNewFileCommand}"/>
                            <MenuItem Header="Paste" Command="{Binding PasteFromClipboardCommand}"/>
                            <Separator/>
                            <MenuItem Header="Refresh" Command="{Binding RefreshCommand}"/>
                            <MenuItem Header="Open in Explorer" Command="{Binding OpenInExplorerCommand}"/>
                        </ContextMenu>
                    </Grid.ContextMenu>
                <TextBlock Text="{Binding ErrorMessage, Mode=OneWay}"
                           Foreground="{DynamicResource PalisadeErrorBrush}"
                           FontSize="13"
                           Margin="15,10"
                           TextWrapping="Wrap"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}, FallbackValue=Collapsed}" />
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Items}" Margin="10"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                  dd:DragDrop.IsDragSource="True"
                                  dd:DragDrop.IsDropTarget="True"
                                  dd:DragDrop.DropHandler="{Binding}"
                                  dd:DragDrop.DragHandler="{Binding}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ContentControl>
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="MouseDoubleClick">
                                            <i:InvokeCommandAction Command="{Binding Path=Data.NavigateIntoFolderCommand, Source={StaticResource proxy}}" CommandParameter="{Binding}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                    <Border Margin="0,10" CornerRadius="4" Width="100" MouseRightButtonUp="Item_MouseRightButtonUp">
                                        <Border.Style>
                                            <Style>
                                                <Style.Triggers>
                                                    <Trigger Property="Border.IsMouseOver" Value="True">
                                                        <Setter Property="Border.Background" Value="{DynamicResource PalisadeCardHoverBrush}" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" Cursor="Hand">
                                            <Image
                                                Source="{Binding IconPath, Converter={StaticResource PathToImage}}"
                                                Width="48"
                                                Height="48"
                                                Stretch="UniformToFill" Margin="5"
                                            />
                                            <TextBlock TextWrapping="NoWrap"
                                                       TextAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"
                                                       Text="{Binding Name, Mode=OneWay}"
                                                       Foreground="{DynamicResource PalisadeTextBrush}"
                                                       HorizontalAlignment="Center"
                                                       Margin="5,0,5,5"
                                                       ToolTip="{Binding Name, Mode=OneWay}" />
                                        </StackPanel>
                                    </Border>
                                </ContentControl>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                </Grid>
            </Border>
        </DockPanel>
    </Border>
</Window>
